<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Panel de Asistencias</title>
<style>
  body {
    background-color: #0D0D0D;
    color: #FFFFFF;
    font-family: 'Segoe UI', sans-serif;
    margin: 0;
    padding: 20px;
  }

  .top-bar {
    display: flex;
    justify-content: flex-end;
    gap: 10px;
    margin-bottom: 20px;
  }

  .top-bar button {
    background-color: #1E1E1E;
    color: #FFFFFF;
    border: 1px solid #00BFFF;
    padding: 10px 20px;
    cursor: pointer;
    border-radius: 4px;
    transition: 0.3s;
  }

  .top-bar button:hover {
    background-color: #00BFFF;
    color: #0D0D0D;
  }

  .section-title {
    text-align: center;
    background-color: #1E1E1E;
    padding: 10px;
    font-weight: bold;
    letter-spacing: 2px;
    border-radius: 4px;
    margin-bottom: 20px;
  }

  .config-panel {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin-bottom: 20px;
  }

  .config-panel button, .config-panel input {
    background-color: #1E1E1E;
    color: #FFFFFF;
    border: 1px solid #2A2A2A;
    padding: 10px;
    border-radius: 4px;
    flex: 1;
  }

  .config-panel button:hover {
    border-color: #00BFFF;
  }

  .search-panel {
    display: flex;
    gap: 10px;
    margin-bottom: 20px;
  }

  .search-panel input, .search-panel button {
    background-color: #1E1E1E;
    color: #FFFFFF;
    border: 1px solid #2A2A2A;
    padding: 10px;
    border-radius: 4px;
    flex: 1;
  }

  .search-panel button:hover {
    border-color: #00BFFF;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    background-color: #1E1E1E;
    border-radius: 4px;
    overflow: hidden;
  }

  th, td {
    padding: 12px;
    text-align: left;
  }

  th {
    background-color: #2A2A2A;
    color: #00BFFF;
  }

  tr:nth-child(even) {
    background-color: #141414;
  }

  .action-btn {
    padding: 8px 12px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    color: #FFFFFF;
  }

  .edit-btn {
    background-color: #00BFFF;
  }

  .delete-btn {
    background-color: #FF007F;
  }

  .action-btn:hover {
    opacity: 0.8;
  }
</style>
</head>
<body>

  <div class="top-bar">
    <button>Twitch</button>
    <button>Kick</button>
    <button>YouTube</button>
  </div>

  <div class="section-title">ASISTENCIAS</div>

  <div class="config-panel">
    <button>Configurar comando de asistencia</button>
    <input type="text" placeholder="Texto para el comando">
    <button>Solo Followers</button>
    <button>Solo Subs</button>
  </div>

  <div class="search-panel">
    <input type="text" placeholder="Buscador de usuarios">
    <button>Ordenar por</button>
    <button>Botón gráfica</button>
  </div>

  <table>
    <thead>
      <tr>
        <th>Usuario</th>
        <th>Asistencias</th>
        <th colspan="2">Acciones</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>XXXTENTACION</td>
        <td>222</td>
        <td><button class="action-btn edit-btn">Editar</button></td>
        <td><button class="action-btn delete-btn">Eliminar</button></td>
      </tr>
    </tbody>
  </table>

</body>
</html>